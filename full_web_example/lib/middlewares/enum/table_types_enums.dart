enum UserTableTypesEnum { photo, name, phone, email, date }

enum DriverTableTypesEnum { photo, name, phone, email, date }

enum OrderTableTypesEnum { question, variant1, variant2, variant3, variant4 }
